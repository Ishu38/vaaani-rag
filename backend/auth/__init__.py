"""Vaaani auth package: SQLite-backed users, email verification, phone OTP,
Google OAuth (optional). Pluggable email/SMS senders so dev runs without
external credentials and production uses real providers via env vars.
"""
